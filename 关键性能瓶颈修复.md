我先并行排查几个关键性能瓶颈。
用户反馈注入2年测试数据后整体卡顿严重，内存占用1G。需要排查原因并给出至少3个优化方案。

让我先分析代码，找出性能瓶颈。从之前的会话知道：
- 注入了5510条任务 + 416条专注历史
- 11个清单、10个标签、5个过滤器

我需要排查：
1. 数据存储/读取方式
2. 渲染逻辑（每次重渲染是否处理全部数据）
3. 搜索逻辑（是否全量扫描）
4. 事件监听器是否有泄漏
5. 自动刷新机制

让我并行调研几个方向。

让我深入研究一下数据刷新机制、`filterTasks` 和 `renderView`。

现在让我检查一下 `filterTasks`、`renderView` 和 `saveData` 的实现。

现在让我来看一下 `saveData`、`scheduleView` 的渲染（它可能渲染了所有的任务），以及月视图。

让我检查一下日程视图（可能会渲染所有任务）和 `getTaskFocusMinutes` 的性能开销。
Now I have a clear picture. Let me also check the schedule view and month view rendering to understand if they're rendering all tasks.

Let me check the schedule view and getTaskFocusMinutes.

Now I have enough information to identify the performance bottlenecks. Let me analyze:

## Performance Issues Found

### 1. **`getTaskFocusMinutes` is O(n) per call, called for every task during render**
- `renderTaskListView`, `renderSearchResults`, and `renderScheduleView` all call `getTaskFocusMinutes(task.id)` for every task
- Each call does `pomodoroHistory.filter()` — full scan of 416 records
- With 5510 tasks, this is 5510 × 416 = ~2.3M operations PER RENDER
- Schedule view renders all tasks across a year — extreme

### 2. **`refreshDataFromServer` every 30s re-renders EVERYTHING**
- Line 103-104: `renderLists()` + `renderView()` on every refresh
- With 5510 tasks, every 30 seconds the entire view is rebuilt
- This causes the "switching views is slow" and jank during background

### 3. **`saveData` sends ENTIRE dataset (5510 tasks + 416 history) every save**
- `_doSaveData` line 387-394: JSON.stringify the entire `tasks` array every save
- Even a single task toggle triggers a full save of all 5510 tasks
- JSON.stringify + network transfer of ~2 years of data is expensive

### 4. **Schedule view renders 1 year (12 months) of tasks in DOM at once**
- Line 320-324: startMonth (current-3) to endMonth (current+9) = ~12 months
- Every task in that range gets a DOM node
- With heavy test data (0-15 tasks/day × 365 days = up to 5000+ DOM nodes)

### 5. **Memory: 1GB usage**
- Each task object + DOM nodes + event listeners
- `renderView` does `container.innerHTML = html` — old DOM removed but if there are lingering references (closures in event listeners), GC can't reclaim
- 5510 tasks × multiple renders without GC = memory bloat

### 6. **`filterTasks` called multiple times per render**
- `renderTaskListView` calls `filterTasks(tasks)` once — OK
- But `renderQuadrantView` calls it twice (line 4 and 185)
- Each filter creates new arrays

### 7. **Command palette search is O(n) with no debounce**
- `searchTasks` scans all 5510 tasks on every keystroke
- `handleCommandPaletteInput` doesn't debounce

### 8. **Background image / color scheme changes trigger full re-render**
- Applying settings calls `renderView()` + `renderLists()` — full DOM rebuild with 5510 tasks

Now let me give the user at least 3 concrete optimization recommendations. The user asked for "建议与方案" (suggestions AND plans), at least 3. They want analysis + solutions, not necessarily implementation yet.

Let me structure the answer:
1. Root cause analysis
2. At least 3 optimization solutions with priority/impact

I should NOT implement yet — the user asked to "排查出现这些现象的原因，给出一些降低卡顿感觉的建议与方案（至少给出3个）". This is a diagnostic + recommendation request.

Let me write a thorough analysis.
我已完成排查。以下是卡顿与内存占用高的根因分析，以及 5 个优化方案（按收益排序）。

## 根因排查

**核心数据规模**：5510 条任务 + 416 条专注历史，且每次渲染/保存都是全量处理。

### 瓶颈 1：`getTaskFocusMinutes` 在渲染中被反复全量扫描（最严重）

[pomodoro.js:1851](file:///d:/Project/TackList/pomodoro.js#L1851-L1853) 每次调用都对 416 条专注历史做 `filter` 全表扫描：

```javascript
function getTaskFocusMinutes(taskId) {
    const taskHistory = pomodoroHistory.filter(p => p.taskId === taskId);  // O(n)
    return taskHistory.reduce((total, record) => total + (record.duration || 0), 0);
}
```

而 [taskListView.js:199](file:///d:/Project/TackList/taskListView.js#L199)、`renderScheduleView`、`renderSearchResults` 都对**每个任务**调用一次。任务列表渲染一次 ≈ 5510 × 416 ≈ **230 万次比较**。日程视图渲染一年任务时更夸张。这是切换视图、搜索、查看任务都卡的主因。

### 瓶颈 2：每 30 秒自动刷新触发全量重渲染

[data.js:82-106](file:///d:/Project/TackList/data.js#L82-L106) `refreshDataFromServer` 拉取全部数据后无条件调用 `renderLists()` + `renderView()`，即使数据未变。每次刷新都重建 5510 条任务的 DOM，主线程阻塞 5s+。期间任何交互（Esc、点击）都排队等待。

### 瓶颈 3：`saveData` 每次保存都序列化全量数据

[data.js:385-398](file:///d:/Project/TackList/data.js#L385-L398) 即使只勾选一个任务，也会 `JSON.stringify` 整个 5510 条任务 + 416 条历史 + 设置，body 体积大，序列化和网络传输都耗时。保存期间 `_saveInFlight = true` 还会阻塞刷新队列，造成连锁卡顿。

### 瓶颈 4：日程视图一次渲染 12 个月 DOM

[taskListView.js:320-324](file:///d:/Project/TackList/taskListView.js#L320-L324) 渲染当前月前 3 个月到后 9 个月（共约 12 个月）的所有任务。重度数据下一天 0-15 条任务，12 个月可达数千 DOM 节点，单次 `innerHTML =` 耗时巨大。

### 瓶颈 5：命令面板搜索无防抖 + 全量扫描

[commandPalette.js:128-134](file:///d:/Project/TackList/commandPalette.js#L128-L134) `searchTasks` 每次按键都对 5510 条任务做 `toLowerCase()` + `includes()`，输入时连续触发，叠加渲染搜索结果 HTML，造成输入卡顿。

### 瓶颈 6：内存 1G 的来源

- 每次全量 `innerHTML =` 替换，旧 DOM 节点若被事件监听器闭包引用（如 `onclick` 内联属性、`addEventListener` 绑定的），GC 难以回收
- `refreshDataFromServer` 每 30s 重新赋值 `tasks = data.tasks`，旧数组若仍被渲染中的闭包持有，堆积不释放
- 日程视图数千 DOM 节点 × 每节点多个内联事件属性，内存占用线性增长

---

## 优化方案（5 个，按收益从高到低）

### 方案 1：构建 `taskId → focusMinutes` 缓存表（收益最高，改动最小）

**思路**：在数据加载/变更时一次性聚合，渲染时 O(1) 查表。

```javascript
let _taskFocusMinutesCache = {};  // taskId -> minutes

function rebuildFocusMinutesCache() {
    _taskFocusMinutesCache = {};
    pomodoroHistory.forEach(p => {
        _taskFocusMinutesCache[p.taskId] = (_taskFocusMinutesCache[p.taskId] || 0) + (p.duration || 0);
    });
}
function getTaskFocusMinutes(taskId) {
    return _taskFocusMinutesCache[taskId] || 0;
}
```

在 `loadData`、`refreshDataFromServer`、`_mergeServerData`、保存专注记录后调用 `rebuildFocusMinutesCache()`。

**收益**：渲染从 230 万次比较降到 5510 次查表，**提速 ~400 倍**。这是消除"切换视图/搜索/查看任务卡"的关键。

### 方案 2：自动刷新做"变更检测 + 视图感知"

**思路 A（数据层）**：`refreshDataFromServer` 拉取后比对版本号/数据指纹，未变则不渲染。

```javascript
async function refreshDataFromServer() {
    if (currentDetailTaskId || _saveInFlight) { _dataRefreshPending = true; return; }
    _dataRefreshPending = false;
    try {
        const response = await fetch('/api/data');
        const data = await response.json();
        // 用版本号或数据哈希判断是否真的变化
        if (data._version === _dataVersion) return;  // 未变，跳过渲染
        // ...原有赋值逻辑
    }
}
```

**思路 B（视图层）**：仅当前视图不可见时（如标签页隐藏）延迟刷新，可见时再执行；或刷新时仅更新数据不立即 `renderView`，等用户下次交互时再渲染。

**收益**：消除后台每 30s 一次的全量渲染卡顿，Esc 关闭命令面板等交互不再被阻塞。

### 方案 3：命令面板搜索加防抖 + 缓存小写化标题

**思路**：输入防抖 150ms，并预计算任务的小写标题/笔记避免每次 `toLowerCase()`。

```javascript
// 数据加载时预计算
let _searchIndex = [];
function rebuildSearchIndex() {
    _searchIndex = tasks.map(t => ({
        id: t.id,
        title: (t.title || '').toLowerCase(),
        notes: (t.notes || '').toLowerCase()
    }));
}

let _searchDebounceTimer = null;
function handleCommandPaletteInput(e) {
    clearTimeout(_searchDebounceTimer);
    _searchDebounceTimer = setTimeout(() => {
        // 用 _searchIndex 而非 tasks 做搜索
        const q = e.target.value.trim().toLowerCase();
        const results = _searchIndex.filter(s => s.title.includes(q) || s.notes.includes(q))
                                    .slice(0, 8)
                                    .map(s => tasks.find(t => t.id === s.id));
        // ...渲染
    }, 150);
}
```

**收益**：搜索输入从每键全量扫描 + `toLowerCase` 5510 次，降到防抖后单次扫描预计算数组，输入流畅。

### 方案 4：`saveData` 改为增量保存

**思路**：服务端增加单任务/单清单的 PATCH 接口，前端只发送变更部分。

```javascript
// 勾选任务时只传该任务
function saveTaskPatch(taskId) {
    const task = tasks.find(t => t.id === taskId);
    return fetch('/api/tasks/' + taskId, {
        method: 'PATCH',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify(task)
    });
}
```

**收益**：单次保存从序列化 5510 条降到 1 条，保存期间不再阻塞刷新队列，勾选/编辑响应即时。

### 方案 5：日程视图虚拟化 / 分页渲染

**思路**：日程视图只渲染当前可见月份 ± 1 个月，滚动到边界时再动态追加；或用 `IntersectionObserver` 懒渲染屏幕外日期。

**收益**：DOM 节点数从数千降到数百，初始渲染和滚动都流畅。改动较大，作为长期优化。

---

## 建议实施顺序

1. **方案 1**（缓存表）— 收益最大、改动最小，优先做
2. **方案 2**（变更检测）— 消除后台卡顿
3. **方案 3**（搜索防抖）— 命令面板立即流畅
4. **方案 4**（增量保存）— 编辑交互即时
5. **方案 5**（视图虚拟化）— 长期优化
