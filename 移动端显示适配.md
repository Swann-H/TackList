# 移动端显示适配需求文档

## 1. 背景与目标

当前系统基于 PC 端横屏环境开发，采用"左侧边栏 + 中间主内容区 + 右侧面板"三栏布局。随着用户在手机浏览器中使用场景增多，需要对整体界面进行移动端适配，确保在 320px~768px 宽度的竖屏设备上可用、易用、视觉舒适。

**目标：**
- 核心功能（任务查看、创建、编辑、日程浏览、番茄专注）在手机上可流畅操作。
- 保留原有主题配色与交互习惯，不破坏 PC 端体验。
- 优先采用响应式改造，尽量避免维护两套独立 UI。

---

## 2. 设计原则

1. **单栏优先：** 手机端默认仅展示一个主视图，左侧边栏以抽屉呈现，右侧任务详情、计划、设置以全屏页呈现，不挤压主内容区。
2. **触控友好：** 按钮、列表项、输入框最小触控区域不小于 44×44px；增加元素间距避免误触。
3. **信息层级清晰：** 减少同一屏内信息密度，重要操作固定或悬浮，次要内容可折叠。
4. **保持品牌一致：** 保留现有主题变量（`--accent-*`、`--bg-*`、`--text-*`）和圆角、阴影风格。
5. **渐进降级：** 复杂视图（周视图、月视图）在窄屏下允许简化展示，保证可读性。

---

## 3. 断点与布局策略

| 断点范围 | 设备类型 | 布局策略 |
|---------|---------|---------|
| `< 640px` | 手机竖屏 | 单栏布局；左侧边栏为抽屉；任务详情、计划、设置均为全屏页；顶部导航使用横向可滚动标签。 |
| `640px~768px` | 手机横屏/小平板 | 单栏布局；左侧边栏为抽屉；任务详情、计划、设置均为全屏页。 |
| `768px~1024px` | 平板 | 双栏布局：侧边栏收窄为图标栏 + 主内容区；任务详情与计划采用右侧滑入抽屉。 |
| `> 1024px` | PC/大屏 | 维持现有三栏布局。 |

> 注：现有代码已在 `max-width: 768px` 下将侧边栏改为左侧抽屉。建议统一以 `768px` 作为移动端/桌面端分界点，并在 `640px` 处增加手机精细适配。

---

## 4. 全局基础适配

### 4.1 视口与缩放
- 保持现有 `viewport`：`<meta name="viewport" content="width=device-width, initial-scale=1.0">`。
- 保留用户缩放能力，不设置 `user-scalable=no`，以满足无障碍需求。
- 全屏页和抽屉的高度统一使用 `100dvh`，避免被移动端浏览器的动态地址栏遮挡。

### 4.2 字体与间距
- 手机端基础字号保持 `14px~16px`，标题字号相对 PC 端下调 10%~20%。
- 主内容区内边距从 `p-6`（24px）调整为 `p-3`（12px）。
- 列表项高度从 PC 端约 48px 提升至 56px，增大触控区域。
- 全局 CSS 添加 `touch-action: manipulation;`，消除移动端默认的 300ms 点击延迟并禁止双击缩放。针对 API 请求（如完成任务、修改状态），添加防抖（Debounce）处理，防止网络不佳时用户多次点击导致 Python 后端处理异常。

### 4.3 滚动与固定
- 主内容区允许垂直滚动，顶部操作栏在滚动时保持固定（sticky），方便随时操作。
- 右下角放置悬浮添加按钮（FAB），用于高频操作"添加任务"。

---

## 5. 各模块适配方案

### 5.1 侧边栏

**现状：** 宽 260px，包含清单、标签、过滤器、假期倒计时、功能按钮、设置。

**手机端方案：**
- 采用左侧抽屉形式，宽度 280px，从 `left: -280px` 滑入至 `left: 0`。
- 抽屉打开时背景增加遮罩（`modal-overlay` 风格），点击遮罩关闭抽屉。
- 顶部标题区增加关闭按钮（`×`）。
- 底部功能按钮（番茄、正念、答案之书、设置）图标化展示，保持现有圆形按钮样式，尺寸放大至 44×44px。
- 清单/标签/过滤器分组可折叠，默认展开"清单"，"标签"与"过滤器"默认折叠，减少首屏长度。

### 5.2 顶部导航

**现状：** 顶部一行展示视图切换（任务/日程/周/月/四象限）、视图导航栏、右侧操作按钮（今天/计划/顺延/添加）。

**手机端方案：**
- **视图切换：** 改为横向可滚动标签栏，标签宽度自适应，当前视图高亮。避免文字换行，标签项高度 40px。
- **视图导航栏（`#view-nav-bar`）：** 日期/周切换按钮保持紧凑，标题文字超长时省略，按钮尺寸 36×36px。
- **右侧操作按钮：**
  - "添加任务"提升为悬浮按钮（FAB），固定于右下角，替代顶部圆形按钮。
  - "今天"、"计划"、"顺延"收入顶部右侧的更多菜单（`⋯`），点击展开。
  - FAB 交互：当页面向下滚动时，FAB 缩小并半透明变淡；向上滚动或停止滚动时恢复；软键盘弹出时自动隐藏，避免遮挡输入内容。

### 5.3 任务列表视图

**现状：** 任务列表占据主内容区，任务项包含复选框、标题、时间、标签、优先级、子任务、专注按钮等。

**手机端方案：**
- 列表项改为卡片式，增加上下内边距至 12px，左右 12px。
- 一行内展示：复选框（44×44px 触控区） + 标题（自适应省略） + 优先级标识。
- 时间、标签、子任务进度等次要信息放在第二行，使用更小字号和 muted 色。
- 右滑操作：
  - 左滑：快速删除。
  - 右滑：快速完成。
  - 优先保证点击复选框可直接完成，避免过度依赖手势。
  - 卡片侧滑操作区域限制在卡片内部，增加滑动阈值判断，避免与系统级边缘返回手势冲突。若滑动冲突严重，降级为"长按弹出底部动作面板（Action Sheet）"作为替代方案。
- 分组标题（如"今天"、"明天"）保持吸顶（sticky），便于定位。
- 任务项内的"开始专注"按钮在移动端始终显示为小图标，尺寸 32×32px。

### 5.4 日程视图

**现状：** 时间轴纵向排列，左侧时刻列，右侧任务卡片按时间分布。

**手机端方案：**
- 时刻列宽度收窄为 48px，字号 12px。
- 任务卡片宽度自适应，左右内边距 8px。
- 全天任务固定在顶部折叠展示，默认展开 2 条，超出显示"+N"。
- 当前时间线（红线）保持显示。
- 支持捏合/双击缩放时间粒度（可选进阶），最低优先级保证 1 小时一格可读。

### 5.5 周视图

**现状：** 横向 7 列 + 左侧时刻列，格子较窄。

**手机端方案：**
- 采用"单日详细视图"：手机端周视图默认展示当前选中日的详细日程。
- 顶部保留 7 天日期选择条（横向可滚动），点击切换日期。
- 支持左右滑动手势切换上一周/下一周，使用轻量级手势库（如 Hammer.js 或 AlloyFinger）或自行封装 `touchstart/touchmove/touchend` 逻辑实现。
- 头部 sticky 保留，确保星期和日期始终可见。
- 全天事件行折叠，默认展开 2 条，超出显示"+N"。

### 5.6 月视图

**现状：** 7×6 或 7×5 网格，每天格内展示任务点/标题。

**手机端方案：**
- 日历网格宽度 100%，日期数字放在左上角，字号 12px。
- 每个日期格内仅显示任务数量点，最多 3 个点，超出显示"+N"。
- 点击日期后，进入全屏页展示当天详细任务列表。
- 月份切换按钮尺寸放大至 40×40px。
- 支持左右滑动手势切换上一月/下一月，使用轻量级手势库或自行封装 touch 事件实现。

### 5.7 四象限视图

**现状：** 2×2 网格展示四个象限。

**手机端方案：**
- 改为纵向排列四个象限卡片，每个卡片可展开/折叠。
- 默认展开"重要且紧急"，其他折叠显示标题和任务数量。
- 卡片内任务列表与任务列表视图手机端样式一致。

### 5.8 摘要视图

**现状：** 展示完成率、最佳专注时间、专注时间线、统计图表等。

**手机端方案：**
- 纵向堆叠各统计卡片，图表宽度 100%。
- 柱状图/折线图在窄屏下减少 X 轴标签密度。
- 数值卡片（如完成率、专注数）改为 2 列网格，避免一行过多导致拥挤。

### 5.9 任务详情面板

**现状：** 右侧固定宽 384px（`w-96`）面板。

**手机端方案：**
- 采用全屏页呈现，从右侧滑入覆盖整个视口。
- 顶部保留关闭按钮（`×`）和标题。
- 表单字段（日期、时间、标签、重复、提醒）纵向排列，输入框宽度 100%。
- 日期/时间选择器优先使用原生 `<input type="date/time">`，避免自定义下拉在手机上被遮挡。
- 删除/保存等操作固定在底部工具条，底部工具条使用 `padding-bottom: env(safe-area-inset-bottom);` 留出全面屏 Home Indicator 安全距离，避免误触。
- 软键盘弹出时，当前输入框通过 `element.scrollIntoView({ behavior: 'smooth', block: 'center' })` 自动滚动到可视区域居中位置。

### 5.10 计划面板

**现状：** 右侧固定宽 384px 面板。

**手机端方案：**
- 与任务详情面板一致，采用全屏页呈现，从右侧滑入。
- 计划任务列表在手机端取消拖拽排序，仅保留勾选完成与删除操作。

### 5.11 番茄专注页

**现状：** 全屏或嵌入页面，展示倒计时、任务选择、历史记录。

**手机端方案：**
- 倒计时数字居中放大，保持 `pomodoro-timer-font` 字体。
- 控制按钮（开始/暂停/停止/跳过）放大至 56×56px，横向居中排列，间距 20px。
- 任务选择下拉/搜索框宽度 100%。
- 历史记录改为底部折叠面板，默认收起，点击展开。
- 右侧滚动条隐藏逻辑在手机端保持透明，滚动时显示，停止滚动 1 秒后自动隐藏。

### 5.12 设置/弹窗

**现状：** 设置以模态框（Modal）展示，内容较多。

**手机端方案：**
- 模态框改为全屏页，从底部滑入，宽度 100%。
- 设置项分组展示，分组标题 sticky。
- 开关、选择器尺寸放大，确保可单手操作。
- 长表单支持分段展示，"返回"、"保存"按钮固定于底部。

---

## 6. 交互适配

### 6.1 添加任务
- 手机端主界面右下角放置圆形 FAB（直径 56px），点击打开全屏任务创建页。
- 快速添加支持语音输入（可选，依赖设备能力）。

### 6.2 搜索与命令面板
- 命令面板（`commandPalette.js`）在手机端改为全屏搜索页，输入框置顶，结果列表可滚动。
- 搜索结果项高度加大至 56px，支持点击执行。

### 6.3 长按/右滑菜单
- 对任务项支持长按弹出操作菜单：编辑、删除、开始专注、推迟、移动清单。
- 手势操作作为辅助，不替代基础点击操作。

### 6.4 返回与关闭
- 所有抽屉、弹窗、详情页提供明确的关闭/返回按钮。
- 支持 Android 物理返回键关闭当前抽屉/弹窗：打开全屏页/抽屉时，利用 `history.pushState` 推入一个虚拟状态；监听 `popstate` 事件，当触发物理返回时，拦截并执行关闭全屏页/抽屉的逻辑，而不是回退浏览器页面。

### 6.5 Hover 状态处理
- 将 PC 端依赖 Hover 触发的操作（如任务项右侧的隐藏按钮），在移动端改为常驻显示（小图标），或通过长按/右滑菜单、点击进入详情页来触发。
- 全面排查 PC 端的 `:hover` 样式，确保移动端无操作歧义。

---

## 7. 技术实现建议

### 7.1 CSS 策略
- 使用 Tailwind CSS 响应式前缀（`sm:`、`md:`、`lg:`）覆盖移动端样式。
- 在现有 `@media (max-width: 768px)` 基础上，新增 `@media (max-width: 640px)` 精细适配。
- 对需要抽屉化的侧边栏使用 `position: fixed` + `transform: translateX(...)`，配合 `transition` 实现平滑动画。
- 对全屏页（任务详情、计划、设置）使用 `position: fixed; inset: 0; z-index: 50`，从右侧或底部滑入。
- 全屏页和抽屉高度统一使用 `100dvh`（Dynamic Viewport Height），避免移动端浏览器动态地址栏导致底部按钮遮挡。
- 底部操作栏（如任务详情保存按钮、设置底部按钮）使用 `padding-bottom: env(safe-area-inset-bottom);` 为全面屏 Home Indicator 留出安全距离。
- 全局添加 `touch-action: manipulation;`，消除移动端 300ms 点击延迟并禁止双击缩放。

### 7.2 JavaScript 适配
- 使用 `window.matchMedia('(max-width: 768px)')` 替代 `window.innerWidth` 监听，性能更好且能自动响应横竖屏切换：
  ```javascript
  const mediaQuery = window.matchMedia('(max-width: 768px)');
  function handleDeviceChange(e) {
      if (e.matches) { /* 切换为移动端 DOM 结构或逻辑 */ }
      else { /* 切换为 PC 端结构 */ }
  }
  mediaQuery.addEventListener('change', handleDeviceChange);
  ```
- 避免为移动端和 PC 端维护两套 DOM。通过 CSS 媒体查询改变同一个 DOM 元素的展现形态：PC 端侧边栏为 `position: relative` 占据宽度，移动端通过 CSS 改为 `position: fixed; transform: translateX(-100%); transition: transform 0.3s; z-index: 50;`，JS 仅负责 `classList.toggle('drawer-open')`。
- 根据设备类型控制侧边栏、详情面板、计划面板的渲染方式：
  - 移动端：侧边栏抽屉，详情/计划/设置全屏页。
  - 桌面端：侧边栏固定，详情/计划右侧面板。
- 对需要原生输入的字段（日期、时间），在手机端使用 `<input type="date">` / `<input type="time">`。

### 7.3 测试建议
- 真机测试：iOS Safari、Android Chrome、Android 微信内置浏览器。
- 覆盖不同分辨率：375px、414px、768px。
- 测试交互：软键盘弹出时布局是否被顶起、日期选择器是否被遮挡、底部固定按钮是否被导航条覆盖。
- 软键盘遮挡专项测试：在 Android 和 iOS 上测试任务编辑页，确保原生键盘弹出时，输入框能通过 `element.scrollIntoView({ behavior: 'smooth', block: 'center' })` 自动滚动到可视区域居中位置。
- 手势冲突测试：验证任务列表侧滑、月/周视图左右滑动与系统级边缘返回手势是否存在冲突。
- 物理返回键测试：验证打开任务详情/设置/抽屉后，点击 Android 物理返回键能正确关闭当前层，而不是退出网页。

---

## 8. 适配优先级

| 优先级 | 模块 | 说明 |
|-------|------|------|
| P0 | 全局布局 + 侧边栏抽屉 | 保证基础导航可用。 |
| P0 | 任务列表 + 任务详情全屏页 | 核心增删改查功能。 |
| P0 | 添加任务 FAB | 高频操作入口。 |
| P1 | 日程视图 + 周视图 + 月视图 | 日历类视图简化展示。 |
| P1 | 番茄专注页 | 专注功能高频使用。 |
| P2 | 四象限 + 摘要 | 复杂视图后续优化。 |
| P2 | 手势/长按/返回键 | 提升操作效率。 |
| P2 | PWA 改造 | 添加到主屏幕，提升移动端启动体验。 |

---

## 9. 验收标准

- [ ] 在 375px 宽手机浏览器中，侧边栏以抽屉形式正常打开/关闭，主内容区不被挤压。
- [ ] 任务列表可正常滑动、点击完成、点击进入详情。
- [ ] 任务详情面板在手机端以全屏页展示，所有字段可编辑并保存。
- [ ] 计划面板在手机端以全屏页展示，可正常勾选与删除计划任务。
- [ ] 设置在手机端以全屏页展示，可正常滚动、关闭、保存。
- [ ] 日程/周/月视图在手机端可读，无严重重叠或截断。
- [ ] 番茄专注页倒计时、控制按钮清晰可见，可正常开始/暂停。
- [ ] 软键盘弹出时，当前输入框不被遮挡。
- [ ] 深色模式与浅色模式在手机端均正常显示。
- [ ] Android 物理返回键能正确关闭当前全屏页/抽屉，而不是退出网页。
- [ ] 全面屏设备底部操作栏与 Home Indicator 无重叠误触。

---

## 10. 进阶规划：PWA（渐进式 Web 应用）

为提升移动端用户体验，使应用可像原生 App 一样添加到手机桌面，建议后续进行 PWA 改造。

### 10.1 改造内容
- 添加 `manifest.json` 文件，定义应用名称、图标、主题色、背景色、启动方式。
- 配置 `"display": "standalone"`，隐藏浏览器地址栏，提供沉浸式无边框体验。
- 配置启动画面（Splash Screen）颜色，与系统主题色保持一致。
- 添加 Service Worker（离线版可复用现有离线资源），支持离线访问与资源缓存。

### 10.2 收益
- 用户可通过 Safari/Chrome 的"添加到主屏幕"将应用安装到桌面。
- 获得接近原生 App 的启动体验和沉浸式界面，无需用户手动输入网址。
- 基于现有 Python + JavaScript 架构，改造成本低，仅需增加少量配置文件和 Service Worker 脚本。

### 10.3 优先级
- 优先级：P2（可选进阶），建议在核心移动端适配完成后再进行。

---

*文档版本：v3.0*  
*更新日期：2026-07-30*  
*适用范围：日程管理系统 Web 端移动端浏览器适配*
